language: ruby, node_js, java, go

rvm:
 - "2.2"

node_js:
 - "5"

before_install:
 - npm install bower
 - npm install semver
# - npm install
# - bundle install

script:
 - bundle exec rake build
 - bundle exec rspec spec